resource_group_name = "DED_Project"

tags = {
  env     = "dev"
  project = "ded"
  owner   = "DED"
}

# leave empty to use default zones; later we can add OpenAI zone if needed
private_dns_zones = []